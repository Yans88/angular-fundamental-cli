import {LengthArticlePipe} from './length-article.pipe';

describe('LengthArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new LengthArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
